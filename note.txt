Initialisation
# git init

Si première fois
# git config --global user.name ""
# git config --global user.email ""

=> Pour savoir "où on en est"
# git status

Versionner
# git add .
# git commit -m "ici le message"

Si besoin retour (ATTENTION SI PAS TROP DE MODIF)
# git reset --hard <commit id>